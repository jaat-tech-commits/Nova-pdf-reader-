name: NOVA PDF AI - Built-in AI APK

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Java 17
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Configure built-in Gemini AI
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          if [ -n "$GEMINI_API_KEY" ]; then
            printf 'GEMINI_API_KEY=%s\n' "$GEMINI_API_KEY" > .env
            echo "Built-in Gemini API key configured."
          else
            cp .env.example .env
            echo "::warning::GEMINI_API_KEY is not configured; building a functional APK without live Gemini AI."
          fi

      - name: Build debug APK
        run: chmod +x ./gradlew && ./gradlew --no-daemon assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v7
        with:
          name: NOVA-PDF-AI-built-in-ai
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
